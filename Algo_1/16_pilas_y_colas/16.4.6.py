# Escribir una función que recibe una expresión matemática (en forma de cade-
# na) y devuelve True si los paréntesis ('()'), corchetes ('[]') y llaves ('{}') están correcta-
# mente balanceados, False en caso contrario. Ejemplos:
# validar('(x+y)/2') -> True
# validar('[8*4(x+y)]+{2/5}') -> True
# validar('(x+y]/2') -> False
# validar('1+)2(+3') -> False